{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "export NODE_ENV=test || \"SET NODE_ENV=test\" && mocha server/**/*.test.js"
  },
  "engines": {
    "node": "10.14.2"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "moment": "^2.24.0",
    "socket.io": "^2.2.0"
  },
  "devDependencies": {
    "expect": "^24.0.0",
    "mocha": "^5.2.0"
  }
}
